[package]
name = "tpcgen-cli"
version = "0.1.0"
authors = { workspace = true }
description = "Command line tool for TPC benchmark data generation."
readme = "README.md"
edition = { workspace = true }
homepage = { workspace = true }
license = { workspace = true }
repository = { workspace = true }

[lib]
name = "tpcgen_cli"
path = "src/lib.rs"

[[bin]]
name = "tpcgen-cli"
path = "bin/tpcgen_cli.rs"

[dependencies]
arrow_59 = { workspace = true, optional = true }
arrow_60 = { workspace = true, optional = true }
clap = { version = "4.5.32", features = ["derive"] }
env_logger = "0.11.7"
futures = "0.3.31"
indicatif = { version = "0.18.3", optional = true }
log = "0.4.26"
num_cpus = "1.0"
parquet_59 = { workspace = true, optional = true }
parquet_60 = { workspace = true, optional = true }
tpcdsgen = { path = "../tpcdsgen", version = "0.1.0" }
tpcdsgen-arrow = { path = "../tpcdsgen-arrow", version = "0.1.0", default-features = false }
tokio = { version = "1.44.1", features = ["full"] }
tpchgen = { path = "../tpchgen", version = "3.0.0" }
tpchgen-arrow = { path = "../tpchgen-arrow", version = "3.0.0", default-features = false }

[features]
default = ["indicatif-progress", "arrow_60"]
# Enable the default CLI terminal progress bars backed by `indicatif`.
indicatif-progress = ["dep:indicatif"]
# Build against arrow/parquet 59. Requires `default-features = false`.
arrow_59 = [
  "dep:arrow_59",
  "dep:parquet_59",
  "tpchgen-arrow/arrow_59",
  "tpcdsgen-arrow/arrow_59",
]
# Build against arrow/parquet 60 (the default).
arrow_60 = [
  "dep:arrow_60",
  "dep:parquet_60",
  "tpchgen-arrow/arrow_60",
  "tpcdsgen-arrow/arrow_60",
]

[dev-dependencies]
assert_cmd = "2.0"
flate2 = "1.1.0"
predicates = "3.0"
tempfile = "3.20.0"
